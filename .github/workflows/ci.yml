name: Cresco Canvas CI

on:
  push:
    branches:
      - commercial/release-quality
  workflow_dispatch:

permissions:
  contents: read

jobs:
  syntax_probe:
    name: Workflow syntax probe
    runs-on: ubuntu-latest
    steps:
      - run: echo "ci workflow parsed"
