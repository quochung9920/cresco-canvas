(()=>{"use strict";document.querySelectorAll('[data-cresco-form]').forEach(form=>{const status=form.querySelector('.cresco-form__status');const submit=form.querySelector('[type="submit"]');const clearErrors=()=>{form.querySelectorAll('[aria-invalid="true"]').forEach(el=>{el.removeAttribute('aria-invalid');el.removeAttribute('aria-describedby')});form.querySelectorAll('.cresco-form__error').forEach(el=>el.remove())};const collect=()=>{const fields={};new FormData(form).forEach((value,key)=>{const match=key.match(/^fields\[([^\]]+)\](\[\])?$/);if(!match)return;const name=match[1];if(match[2]){fields[name]=fields[name]||[];fields[name].push(String(value))}else fields[name]=String(value)});return fields};form.addEventListener('submit',async event=>{event.preventDefault();clearErrors();if(!form.reportValidity())return;submit?.setAttribute('disabled','disabled');form.setAttribute('aria-busy','true');if(status)status.textContent='Submitting…';try{const response=await fetch(form.dataset.endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({payload:form.dataset.payload,signature:form.dataset.signature,website:form.querySelector('[name="website"]')?.value||'',fields:collect()})});const data=await response.json();if(!response.ok||!data.success){Object.entries(data.errors||{}).forEach(([name,message])=>{const field=form.querySelector(`[name="fields[${CSS.escape(name)}]"],[name="fields[${CSS.escape(name)}][]"]`);if(!field)return;const id=`cresco-error-${name}-${Math.random().toString(36).slice(2,8)}`;field.setAttribute('aria-invalid','true');field.setAttribute('aria-describedby',id);const error=document.createElement('p');error.id=id;error.className='cresco-form__error';error.textContent=String(message);field.closest('.cresco-form-field')?.append(error)});throw new Error(data.message||'Please review the form and try again.')}if(status)status.textContent=data.message||'Thank you.';form.reset();if(data.redirectUrl)window.location.assign(data.redirectUrl)}catch(error){if(status)status.textContent=error.message||'Submission failed. Please try again.'}finally{submit?.removeAttribute('disabled');form.removeAttribute('aria-busy')}})})})();
