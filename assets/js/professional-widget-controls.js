(function(window,document){
'use strict';
var root=document.getElementById('cresco-canvas-standalone-editor');if(!root)return;var scheduled=false;
var BORDER_FIELDS={width:'Border width',style:'Border style',color:'Border color',radius:'Border radius'};
var STYLE_VALUES=['none','solid','dashed','dotted','double','groove','ridge','inset','outset','hidden'];
function all(selector,scope){return Array.prototype.slice.call((scope||document).querySelectorAll(selector));}
function one(selector,scope){return(scope||document).querySelector(selector);}
function norm(value){return String(value||'').replace(/\s+/g,' ').trim().toLowerCase();}
function fieldLabel(field){var label=one('.cc-builder-style-label label, :scope > label',field);return norm(label&&label.textContent);}
function findStyleField(label){return all('.cc-builder-style-field',root).find(function(field){return fieldLabel(field)===norm(label);})||null;}
function nativeSet(input,value){if(!input)return;var proto=input instanceof window.HTMLSelectElement?window.HTMLSelectElement.prototype:window.HTMLInputElement.prototype;var desc=Object.getOwnPropertyDescriptor(proto,'value');if(desc&&desc.set)desc.set.call(input,value);else input.value=value;input.dispatchEvent(new window.Event('input',{bubbles:true}));input.dispatchEvent(new window.Event('change',{bubbles:true}));}
function splitCss(value){var input=String(value||'').trim(),parts=[],buf='',depth=0,quote='';for(var i=0;i<input.length;i++){var ch=input[i];if(quote){buf+=ch;if(ch===quote&&input[i-1]!=='\\')quote='';continue;}if(ch==='"'||ch==="'"){quote=ch;buf+=ch;continue;}if(ch==='(')depth++;if(ch===')'&&depth>0)depth--;if(/\s/.test(ch)&&depth===0){if(buf.trim()){parts.push(buf.trim());buf='';}continue;}buf+=ch;}if(buf.trim())parts.push(buf.trim());return parts;}
function expandSides(value,radius){var raw=String(value||'').trim();if(!raw)return['','','',''];if(radius&&raw.indexOf('/')!==-1)raw=raw.split('/')[0].trim();var p=splitCss(raw);if(!p.length)return['','','',''];if(p.length===1)return[p[0],p[0],p[0],p[0]];if(p.length===2)return radius?[p[0],p[1],p[0],p[1]]:[p[0],p[1],p[0],p[1]];if(p.length===3)return radius?[p[0],p[1],p[2],p[1]]:[p[0],p[1],p[2],p[1]];return p.slice(0,4);}
function compact(values,radius,linked,kind){var v=values.map(function(x){return String(x||'').trim();});if(linked)return v.find(Boolean)||'';if(v.every(function(x){return!x;}))return'';var fallback=kind==='color'?'transparent':kind==='style'?'none':'0';var fill=v.map(function(x){return x||fallback;});if(radius){if(fill[0]===fill[1]&&fill[0]===fill[2]&&fill[0]===fill[3])return fill[0];if(fill[0]===fill[2]&&fill[1]===fill[3])return fill[0]+' '+fill[1];if(fill[1]===fill[3])return fill[0]+' '+fill[1]+' '+fill[2];return fill.join(' ');}if(fill[0]===fill[1]&&fill[0]===fill[2]&&fill[0]===fill[3])return fill[0];if(fill[0]===fill[2]&&fill[1]===fill[3])return fill[0]+' '+fill[1];if(fill[1]===fill[3])return fill[0]+' '+fill[1]+' '+fill[2];return fill.join(' ');}
function contextKey(){var id=one('.cc-builder-panel-head small',root),device=one('.cc-builder-device-tabs .is-active',root),state=one('.cc-builder-state-tabs .is-active',root);return[text(id&&id.textContent),text(device&&device.textContent),text(state&&state.textContent)].join('|');}
function text(value){return String(value||'').trim();}
function makeInput(kind,value,label){var input;if(kind==='style'){input=document.createElement('select');STYLE_VALUES.forEach(function(optionValue){var option=document.createElement('option');option.value=optionValue;option.textContent=optionValue;input.appendChild(option);});if(STYLE_VALUES.indexOf(value)===-1){var custom=document.createElement('option');custom.value=value;custom.textContent=value||'inherit';input.insertBefore(custom,input.firstChild);}input.value=value;}else{input=document.createElement('input');input.type='text';input.value=value;input.placeholder='inherit';}input.setAttribute('aria-label',label);return input;}
function buildGroup(name,title,original,radius){var raw=one('.cc-builder-style-row input',original);if(!raw)return null;var values=expandSides(raw.value,radius),linked=splitCss(raw.value).length<=1&&!String(raw.value||'').includes('/');var section=document.createElement('section');section.className='cc-pro-border-group';section.dataset.group=name;var head=document.createElement('header');var strong=document.createElement('strong');strong.textContent=title;var link=document.createElement('button');link.type='button';link.className='cc-pro-border-link';link.setAttribute('aria-pressed',linked?'true':'false');function paint(){link.textContent=linked?'🔗 Linked':'🔓 Individual';link.classList.toggle('is-linked',linked);}paint();head.appendChild(strong);head.appendChild(link);section.appendChild(head);var labels=radius?['Top left','Top right','Bottom right','Bottom left']:['Top','Right','Bottom','Left'];var grid=document.createElement('div');grid.className='cc-pro-border-grid';var inputs=[];labels.forEach(function(label,index){var wrap=document.createElement('label');var side=document.createElement('span');side.textContent=label;var input=makeInput(name,values[index],title+' '+label);input.addEventListener('input',function(){if(linked){inputs.forEach(function(other){if(other!==input)other.value=input.value;});}write();});input.addEventListener('change',write);inputs.push(input);wrap.appendChild(side);wrap.appendChild(input);grid.appendChild(wrap);});section.appendChild(grid);function write(){nativeSet(raw,compact(inputs.map(function(input){return input.value;}),radius,linked,name));}link.addEventListener('click',function(){linked=!linked;if(linked){var source=inputs.find(function(input){return String(input.value||'').trim()!=='';})||inputs[0];inputs.forEach(function(input){input.value=source.value;});write();}paint();});raw.addEventListener('input',function(){var next=expandSides(raw.value,radius);inputs.forEach(function(input,index){if(document.activeElement!==input)input.value=next[index];});});return section;}
function enhanceBorder(){var styleGrid=one('.cc-builder-style-grid',root);if(!styleGrid)return;var originals={};Object.keys(BORDER_FIELDS).forEach(function(key){originals[key]=findStyleField(BORDER_FIELDS[key]);});if(!originals.width||!originals.style||!originals.color||!originals.radius)return;var key=contextKey(),existing=one('.cc-pro-border-control',styleGrid);if(existing&&existing.dataset.contextKey===key)return;if(existing)existing.remove();Object.keys(originals).forEach(function(k){originals[k].classList.add('cc-pro-border-original');});var panel=document.createElement('section');panel.className='cc-pro-border-control';panel.dataset.contextKey=key;var title=document.createElement('div');title.className='cc-pro-border-title';title.innerHTML='<strong>Border</strong><small>Link all sides for a simple value, or edit each side/corner independently.</small>';panel.appendChild(title);panel.appendChild(buildGroup('width','Width',originals.width,false));panel.appendChild(buildGroup('style','Style',originals.style,false));panel.appendChild(buildGroup('color','Color',originals.color,false));panel.appendChild(buildGroup('radius','Radius',originals.radius,true));styleGrid.insertBefore(panel,originals.color);}
function enhanceProfessionalHint(){var inspector=one('.cc-builder-inspector',root);if(!inspector)return;var head=one('.cc-builder-panel-head strong',inspector),id=one('.cc-builder-panel-head small',inspector);if(!head||!id)return;var label=text(head.textContent).replace(/^Edit\s+/,'');var nested=['Carousel','Slides / Hero Slider','Testimonial Carousel','Logo Carousel','Media Carousel','Infinite Marquee','Timeline','Pricing Table','Modal / Popup','Off Canvas','Hotspot Image','Flip Card'];var existing=one('.cc-pro-widget-hint',inspector);if(nested.indexOf(label)===-1){if(existing)existing.remove();return;}if(existing&&existing.dataset.widgetId===text(id.textContent))return;if(existing)existing.remove();var hint=document.createElement('div');hint.className='cc-pro-widget-hint';hint.dataset.widgetId=text(id.textContent);hint.innerHTML='<strong>Nested professional widget</strong><span>Add child widgets while this widget is selected. Each direct child becomes an item/slide.</span>';var tabs=one('.cc-builder-inspector-tabs',inspector);if(tabs&&tabs.parentNode)tabs.parentNode.insertBefore(hint,tabs);}
function enhanceMediaButtons(){all('.cc-builder-fields .cc-builder-field',root).forEach(function(field){if(field.dataset.ccProMedia==='1')return;var label=one(':scope > label',field),name=norm(label&&label.textContent);if(['before image','after image','background image'].indexOf(name)===-1)return;var input=one('input[type="url"],input[type="text"]',field);if(!input||!window.wp||!window.wp.media)return;field.dataset.ccProMedia='1';var button=document.createElement('button');button.type='button';button.className='cc-builder-secondary cc-pro-media-button';button.textContent='Media Library';button.addEventListener('click',function(){var frame=window.wp.media({title:'Choose image',button:{text:'Use image'},multiple:false});frame.on('select',function(){var model=frame.state().get('selection').first();if(model)nativeSet(input,model.toJSON().url||'');});frame.open();});field.appendChild(button);});all('.cc-builder-json-field',root).forEach(function(field){if(field.dataset.ccProGallery==='1')return;var label=one(':scope > label',field);if(norm(label&&label.textContent)!=='images'||!window.wp||!window.wp.media)return;var head=one('.cc-builder-panel-head strong',root);if(norm(head&&head.textContent)!=='edit image carousel')return;var textarea=one('textarea',field);if(!textarea)return;field.dataset.ccProGallery='1';var button=document.createElement('button');button.type='button';button.className='cc-builder-secondary cc-pro-media-button';button.textContent='Choose carousel images';button.addEventListener('click',function(){var frame=window.wp.media({title:'Choose carousel images',button:{text:'Use images'},multiple:true});frame.on('select',function(){var items=frame.state().get('selection').map(function(model){var a=model.toJSON();return{url:a.url||'',alt:a.alt||'',caption:a.caption||''};});nativeSet(textarea,JSON.stringify(items,null,2));textarea.dispatchEvent(new window.Event('blur',{bubbles:true}));});frame.open();});field.appendChild(button);});}
function run(){scheduled=false;enhanceBorder();enhanceProfessionalHint();enhanceMediaButtons();}
function schedule(){if(scheduled)return;scheduled=true;window.requestAnimationFrame(run);}var observer=new MutationObserver(schedule);observer.observe(root,{childList:true,subtree:true});root.addEventListener('click',function(){window.setTimeout(schedule,0);});root.addEventListener('change',schedule);schedule();
})(window,document);
