import './apply-widget-control-integrity.mjs';
import fs from 'node:fs';
import path from 'node:path';

const root = process.cwd();
const files = [
  path.join(root, 'runtime-src/build/website-builder-studio.js'),
  path.join(root, 'build/website-builder-studio.js'),
];

function replaceOnce(source, before, after, label) {
  if (source.includes(after)) return source;
  if (!source.includes(before)) throw new Error(`Missing ${label}`);
  if (source.indexOf(before) !== source.lastIndexOf(before)) throw new Error(`Ambiguous ${label}`);
  return source.replace(before, after);
}

for (const file of files) {
  let source = fs.readFileSync(file, 'utf8');

  const oldEffective = "function effectiveStyle(node,device,state,global){var style=Object.assign({},obj(node.style)),order=['desktop','laptop','tablet','mobile'];if(device!=='wide'){for(var i=0;i<order.length;i++){Object.assign(style,obj(obj(node.responsive)[order[i]]));if(order[i]===device)break}}if(state&&state!=='normal')Object.assign(style,obj(obj(node.states)[state]));Object.keys(style).forEach(function(k){style[k]=resolveToken(style[k],global)});return style}";
  const semanticEffective = "function semanticStyle(node){var type=String(node&&node.type||''),props=obj(node&&node.props),style={};if(type==='container'){var layout=['block','flex','grid'].indexOf(props.layout)>=0?props.layout:'flex';style={display:layout,width:'auto'};if(props.contentWidth==='boxed'){style.width='100%';style.maxWidth='{layout.containerMax}';style.marginLeft='auto';style.marginRight='auto'}if(layout==='flex'){style.flexDirection=props.direction||'column';style.flexWrap=props.wrap||'nowrap';style.alignItems=props.align||'stretch';style.justifyContent=props.justify||'flex-start'}if(layout==='grid'){style.gridTemplateColumns=String(props.gridTemplate||'').trim()||('repeat('+Math.min(12,Math.max(1,Number(props.columns||2)))+',minmax(0,1fr))')}return style}if(type==='columns')return{display:'grid',gridTemplateColumns:'repeat('+Math.min(12,Math.max(1,Number(props.columns||2)))+',minmax(0,1fr))',gap:'{layout.gridGap}'};if(type==='spacer')return{minHeight:String(props.height||'48px')};return style}\nfunction effectiveStyle(node,device,state,global){var style=Object.assign({},semanticStyle(node),obj(node.style)),order=['desktop','laptop','tablet','mobile'];if(device!=='wide'){for(var i=0;i<order.length;i++){Object.assign(style,obj(obj(node.responsive)[order[i]]));if(order[i]===device)break}}if(state&&state!=='normal')Object.assign(style,obj(obj(node.states)[state]));Object.keys(style).forEach(function(k){style[k]=resolveToken(style[k],global)});return style}";
  source = replaceOnce(source, oldEffective, semanticEffective, 'semantic Studio preview style');

  const unsafeOwnership = "function ownedStyleKeys(def){var owned={};Object.keys(obj(def&&def.props)).forEach(function(key){var styleKey=obj(def.props[key]).styleKey;if(styleKey)owned[String(styleKey)]=true});return owned}\nfunction styleKeysFor(tabName){var defs=selectedNodes.length?selectedNodes.map(function(node){return catalog[node.type]||{}}):[catalog[primary&&primary.type]||{}],owned={};defs.forEach(function(def){Object.assign(owned,ownedStyleKeys(def))});return arr(STYLE_GROUPS[tabName]).filter(function(key){return!owned[key]&&defs.every(function(def){return arr(def.style).indexOf(key)>=0})})}";
  const compilerBackedOwnership = "function semanticOwnedStyleKeys(node){var owned={};if(!node||node.type!=='container')return owned;var def=catalog[node.type]||{};Object.keys(obj(def.props)).forEach(function(key){var styleKey=obj(def.props[key]).styleKey;if(styleKey)owned[String(styleKey)]=true});return owned}\nfunction styleKeysFor(tabName){var selection=selectedNodes.length?selectedNodes:[primary].filter(Boolean),owned={};selection.forEach(function(node){Object.assign(owned,semanticOwnedStyleKeys(node))});return arr(STYLE_GROUPS[tabName]).filter(function(key){return!owned[key]&&selection.length&&selection.every(function(node){var def=catalog[node.type]||{};return arr(def.style).indexOf(key)>=0})})}";
  source = replaceOnce(source, unsafeOwnership, compilerBackedOwnership, 'compiler-backed style ownership');

  const wrongStringListOrder = "else if(type==='int'||type==='number'||kind==='number')control=h('input',{type:'number',min:schema.min,max:schema.max,step:type==='int'?1:'any',value:value==null?'':value,onChange:function(e){var raw=e.target.value;if(raw==='')return;prop(key,coerceProp(schema,raw))},onBlur:function(e){if(e.target.value==='')prop(key,schema.default!=null?schema.default:(schema.min!=null?schema.min:0))}});else if(type==='text'||type==='richtext'||kind==='textarea'||kind==='richtext')control=h('textarea',{rows:type==='richtext'?7:4,value:value==null?'':String(value),placeholder:schema.placeholder||'',onChange:function(e){prop(key,e.target.value)}});else if(type==='json')control=rawJsonControl(key,schema,value);else if(type==='string_list')control=listControl(key,schema,value);";
  const correctStringListOrder = "else if(type==='int'||type==='number'||kind==='number')control=h('input',{type:'number',min:schema.min,max:schema.max,step:type==='int'?1:'any',value:value==null?'':value,onChange:function(e){var raw=e.target.value;if(raw==='')return;prop(key,coerceProp(schema,raw))},onBlur:function(e){if(e.target.value==='')prop(key,schema.default!=null?schema.default:(schema.min!=null?schema.min:0))}});else if(type==='string_list')control=listControl(key,schema,value);else if(type==='text'||type==='richtext'||kind==='textarea'||kind==='richtext')control=h('textarea',{rows:type==='richtext'?7:4,value:value==null?'':String(value),placeholder:schema.placeholder||'',onChange:function(e){prop(key,e.target.value)}});else if(type==='json')control=rawJsonControl(key,schema,value);";
  source = replaceOnce(source, wrongStringListOrder, correctStringListOrder, 'string_list control ordering');

  const objectOnlyRepeater = "function jsonRepeaterControl(key,schema,value){var values=arr(value),shape=String(schema.shape||'');function update(next){prop(key,next)}return h('div',{className:'cc-studio-repeater','data-cresco-repeater-shape':shape},values.map(function(item,index){var row=obj(item);return h('article',{key:index,className:'cc-studio-repeater-card'},h('header',null,h('strong',null,(schema.label||key)+' '+(index+1)),h('span',null,iconButton('arrow-up-alt2','Move up',function(){if(index<1)return;var next=values.slice(),hold=next[index-1];next[index-1]=next[index];next[index]=hold;update(next)},{disabled:index<1}),iconButton('arrow-down-alt2','Move down',function(){if(index>=values.length-1)return;var next=values.slice(),hold=next[index+1];next[index+1]=next[index];next[index]=hold;update(next)},{disabled:index>=values.length-1}),iconButton('trash','Remove',function(){update(values.filter(function(_,i){return i!==index}))},{className:'cc-studio-icon-btn is-danger'}))),Object.keys(row).map(function(field){return h('label',{key:field,className:'cc-studio-repeater-field'},h('small',null,humanize(field)),repeaterItemControl(shape,field,row[field],function(nextValue){var next=values.map(function(entry,i){return i===index?Object.assign({},obj(entry),{[field]:nextValue}):entry});update(next)}))}))}),h('button',{type:'button',className:'cc-studio-button is-secondary',onClick:function(){update(values.concat([repeaterTemplate(schema)]))}},icon('plus-alt2'),' Add '+(schema.label||'item')))}";
  const primitiveSafeRepeater = "function jsonRepeaterControl(key,schema,value){var values=arr(value),shape=String(schema.shape||'');function update(next){prop(key,next)}return h('div',{className:'cc-studio-repeater','data-cresco-repeater-shape':shape},values.map(function(item,index){if(!item||typeof item!=='object'||Array.isArray(item))return h('div',{key:index,className:'cc-studio-repeater-row'},h('input',{value:String(item==null?'':item),onChange:function(e){var next=values.slice();next[index]=e.target.value;update(next)}}),iconButton('arrow-up-alt2','Move up',function(){if(index<1)return;var next=values.slice(),hold=next[index-1];next[index-1]=next[index];next[index]=hold;update(next)},{disabled:index<1}),iconButton('arrow-down-alt2','Move down',function(){if(index>=values.length-1)return;var next=values.slice(),hold=next[index+1];next[index+1]=next[index];next[index]=hold;update(next)},{disabled:index>=values.length-1}),iconButton('trash','Remove',function(){update(values.filter(function(_,i){return i!==index}))},{className:'cc-studio-icon-btn is-danger'}));var row=obj(item);return h('article',{key:index,className:'cc-studio-repeater-card'},h('header',null,h('strong',null,(schema.label||key)+' '+(index+1)),h('span',null,iconButton('arrow-up-alt2','Move up',function(){if(index<1)return;var next=values.slice(),hold=next[index-1];next[index-1]=next[index];next[index]=hold;update(next)},{disabled:index<1}),iconButton('arrow-down-alt2','Move down',function(){if(index>=values.length-1)return;var next=values.slice(),hold=next[index+1];next[index+1]=next[index];next[index]=hold;update(next)},{disabled:index>=values.length-1}),iconButton('trash','Remove',function(){update(values.filter(function(_,i){return i!==index}))},{className:'cc-studio-icon-btn is-danger'}))),Object.keys(row).map(function(field){return h('label',{key:field,className:'cc-studio-repeater-field'},h('small',null,humanize(field)),repeaterItemControl(shape,field,row[field],function(nextValue){var next=values.map(function(entry,i){if(i!==index)return entry;var patched=Object.assign({},obj(entry));patched[field]=nextValue;return patched});update(next)}))}))}),h('button',{type:'button',className:'cc-studio-button is-secondary',onClick:function(){update(values.concat([repeaterTemplate(schema)]))}},icon('plus-alt2'),' Add '+(schema.label||'item')))}";
  source = replaceOnce(source, objectOnlyRepeater, primitiveSafeRepeater, 'primitive-safe repeater');

  fs.writeFileSync(file, source);
}

console.log('Finalized safe WidgetCatalog-driven Inspector migration.');
